// Landing page renders via the (marketing) layout via a redirect,
// but we keep it at the root so "/" works without a route group wrapper.
export { default } from '@/components/landing/LandingPage';
